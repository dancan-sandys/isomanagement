# FastAPI and ASGI server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database (updated for Python 3.13 compatibility)
sqlalchemy==2.0.42
alembic==1.12.1
# psycopg2-binary==2.9.9  # Only needed for PostgreSQL in production

# Authentication and security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6

# Pydantic for data validation (versions with Python 3.13 wheels)
pydantic==2.10.4
pydantic-settings==2.5.2

# File handling and storage
python-magic==0.4.27
boto3==1.34.0
# Bump Pillow for Python 3.13 compatibility
Pillow==10.4.0

# Email and notifications
fastapi-mail==1.4.1

# Date and time handling
python-dateutil==2.8.2

# Excel and PDF generation (pandas removed to avoid compilation issues on Digital Ocean)
openpyxl==3.1.2
reportlab==4.0.7
# pandas==2.0.3  # Commented out - causing compilation failures on Digital Ocean

# QR code generation
qrcode[pil]==7.4.2

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
psutil==5.9.8

# Development tools
python-dotenv==1.0.0
black==23.11.0
isort==5.12.0
flake8==6.1.0

# Monitoring and logging
structlog==23.2.0

# CORS
fastapi-cors==0.0.6 